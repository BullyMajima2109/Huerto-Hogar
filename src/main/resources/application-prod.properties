# Configuración del servidor
server.port=8080
spring.application.name=huerto-hogar-backend

# Base de datos PostgreSQL (producción)
spring.datasource.url=jdbc:postgresql://localhost:5432/huerto_hogar_db
spring.datasource.username=huerto_user
spring.datasource.password=Huerto2024!
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.connection-timeout=5000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
jwt.secret=HuertoHogarSecretKey2024SecureTokenGenerationForJWT
jwt.expiration=86400000

# CORS
spring.web.cors.allowed-origins=http://34.205.67.150,http://34.205.67.150:80,http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# Swagger / OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

